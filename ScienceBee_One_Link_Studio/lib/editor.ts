import OpenAI from "openai";
const client=()=>new OpenAI({apiKey:process.env.OPENAI_API_KEY});
const SYSTEM=`You are the senior Bengali science and technology editor for Science Bee Bangladesh. Write natural Bangladesh Bengali, never literal machine translation. Preserve facts, names, dates and numbers. Use correct Unicode Bengali যুক্তবর্ণ. Avoid academic stiffness, generic AI phrases, exaggerated claims and clickbait. Create a short strong headline, one useful supporting line, and a human editorial caption of 10–15 coherent Bengali sentences. The caption must contain the key information and context from the article, but must not invent facts. Yellow phrases must be exact substrings of the headline or supporting line. Also produce concise English image-search keywords. Return JSON only.`;
export async function makeEditorial(url:string){const r=await client().responses.create({model:process.env.OPENAI_MODEL||"gpt-5.6-luna",input:[{role:"system",content:SYSTEM},{role:"user",content:`Read and verify this news story using web search. URL: ${url}\n\nReturn JSON with: headline_bn, subheadline_bn, yellow_phrases, caption_bn (10–15 sentences), source_label, category, confidence, needs_editor_review, reason_for_review, visual_concept, image_search_query.`}],tools:[{type:"web_search"}]});let s=r.output_text.trim().replace(/^```json\s*/,'').replace(/```$/,'').trim();return JSON.parse(s)}