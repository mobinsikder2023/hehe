"use client";import {useState} from 'react';import {supabaseBrowser} from '@/lib/supabase-browser';import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const r=useRouter();async function go(e:any){e.preventDefault();setBusy(true);setError('');const {error}=await supabaseBrowser().auth.signInWithPassword({email,password});if(error)setError(error.message);else r.replace('/');setBusy(false)}return <main className="login"><form className="loginbox" onSubmit={go}><img className="loginlogo" src="/assets/logo_light.png"/><div className="eyebrow">SCIENCE BEE · EDITORIAL TOOL</div><h1>One-Link Studio</h1><input type="email" placeholder="Team email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<div className="error">{error}</div>}<button disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form></main>}
