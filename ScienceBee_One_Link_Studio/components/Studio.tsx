"use client";import {useState} from 'react';import {supabaseBrowser} from '@/lib/supabase-browser';import {DEFAULT_DESIGN,Design,Candidate} from '@/lib/types';
export default function Studio({userEmail}:{userEmail:string}){const [url,setUrl]=useState(''),[busy,setBusy]=useState(false),[msg,setMsg]=useState(''),[post,setPost]=useState<any>(null),[caption,setCaption]=useState('');const [design,setDesign]=useState<Design>(DEFAULT_DESIGN);const [cands,setCands]=useState<Candidate[]>([]);const [share,setShare]=useState('');
const patch=(k:any,v:any)=>setDesign(x=>({...x,[k]:v}));
async function generate(){if(!url.trim())return;setBusy(true);setMsg('Article পড়ছি, fact-checking করছি, Bengali editorial copy লিখছি…');setShare('');try{const r=await fetch('/api/create',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url})});const j=await r.json();if(!r.ok)throw Error(j.error||'Generation failed');setPost(j.post);setCaption(j.post.caption);setDesign({...DEFAULT_DESIGN,...j.post.design});setCands(j.candidates||[]);setMsg('Draft ready. Review the copy and image, then render.')}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}
async function render(){if(!post)return;setBusy(true);setMsg('Rendering 4K poster…');try{const r=await fetch('/api/render',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:post.id,headline:post.headline_bn,subheadline:post.subheadline_bn,source:post.source_label,phrases:post.yellow_phrases,design})});const j=await r.json();if(!r.ok)throw Error(j.error||'Render failed');setPost((p:any)=>({...p,poster_url:j.poster_url}));setShare(j.share_url||'');setMsg('Poster saved. Share link is ready.')}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}
async function logout(){await supabaseBrowser().auth.signOut();location.href='/login'}
return <div className="shell"><header className="topbar"><div className="brand"><img src="/assets/logo_light.png"/><div><strong>Science Bee One-Link Studio</strong><span>Editorial automation · 4:5 · 2160×2700</span></div></div><div className="userbar">{userEmail}<button className="logout" onClick={logout}>Logout</button></div></header><main className="main"><section className="hero"><div className="eyebrow">ONE LINK → ONE COMPLETE CONTENT PACKAGE</div><h1>Turn a science/tech news link into a polished Bengali post.</h1><p>Humanized headline, supporting line, image selection, 10–15 sentence caption and editable poster.</p><div className="linkrow"><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="Paste a Prothom Alo, Daily Star, Reuters, BBC, Nature, etc. article URL…"/><button className="primary" onClick={generate} disabled={busy}>{busy?'Working…':'Generate'}</button></div><div className="muted" style={{marginTop:8}}>{msg}</div></section>{post&&<section className="workspace"><div className="panel"><h3>Editorial content</h3><div className="field"><label>Main Bengali headline</label><textarea value={post.headline_bn} onChange={e=>setPost({...post,headline_bn:e.target.value})}/></div><div className="field"><label>Important supporting line</label><textarea value={post.subheadline_bn} onChange={e=>setPost({...post,subheadline_bn:e.target.value})}/></div><div className="field"><label>Yellow emphasis</label><input value={(post.yellow_phrases||[]).join(', ')} onChange={e=>setPost({...post,yellow_phrases:e.target.value.split(',').map((x:string)=>x.trim()).filter(Boolean)})}/></div><div className="field"><label>Source</label><input value={post.source_label} onChange={e=>setPost({...post,source_label:e.target.value})}/></div><div className="section"><div className="sectiontitle"><span>Caption · 10–15 sentences</span><button className="btn" onClick={()=>navigator.clipboard.writeText(caption)}>Copy</button></div><div className="captionbox" style={{marginTop:7}}>{caption}</div></div><div className="section"><div className="sectiontitle"><span>Image</span><span className="muted">Replace manually if needed</span></div><input type="file" accept="image/*" style={{width:'100%',marginTop:7}} onChange={async e=>{const f=e.target.files?.[0];if(!f||!post)return;const fd=new FormData();fd.append('file',f);fd.append('id',post.id);const r=await fetch('/api/upload',{method:'POST',body:fd});const j=await r.json();if(r.ok)setPost({...post,image_url:j.image_url})}}/><div className="candidates">{cands.map((c,i)=><button key={i} className="candidate" onClick={()=>setPost({...post,image_url:c.url})}><img src={c.url}/><span>{c.label}</span></button>)}</div></div><div className="section"><div className="sectiontitle"><span>Typography & composition</span><span className="muted">poster pixels</span></div><div className="row2"><div className="field"><label>Composition</label><select value={design.composition} onChange={e=>patch('composition',e.target.value)}><option value="image_first">Image-first · text bottom</option><option value="text_first">Text-first · text top</option></select></div><div className="field"><label>Bottom distance</label><input type="number" value={design.text_bottom} onChange={e=>patch('text_bottom',+e.target.value)}/></div></div><div className="row3"><div className="field"><label>Headline size (0=auto)</label><input type="number" value={design.headline_font_size||0} onChange={e=>patch('headline_font_size',+e.target.value||null)}/></div><div className="field"><label>Headline X</label><input type="number" value={design.headline_x} onChange={e=>patch('headline_x',+e.target.value)}/></div><div className="field"><label>Headline Y</label><input type="number" value={design.headline_top} onChange={e=>patch('headline_top',+e.target.value)}/></div></div><div className="row3"><div className="field"><label>Headline width</label><input type="number" value={design.headline_width} onChange={e=>patch('headline_width',+e.target.value)}/></div><div className="field"><label>Subheading size</label><input type="number" value={design.subheadline_font_size||0} onChange={e=>patch('subheadline_font_size',+e.target.value||null)}/></div><div className="field"><label>Subheading Y</label><input type="number" value={design.subheadline_y} onChange={e=>patch('subheadline_y',+e.target.value)}/></div></div><div className="row3"><div className="field"><label>Fade length</label><input type="number" value={design.fade_length} onChange={e=>patch('fade_length',+e.target.value)}/></div><div className="field"><label>Darkening</label><input type="number" min="0" max=".35" step=".01" value={design.darkening} onChange={e=>patch('darkening',+e.target.value)}/></div><div className="field"><label>Shadow color</label><input value={design.shadow_color} onChange={e=>patch('shadow_color',e.target.value)}/></div></div><div className="row3"><div className="field"><label>Source size</label><input type="number" value={design.source_font_size} onChange={e=>patch('source_font_size',+e.target.value)}/></div><div className="field"><label>Source X</label><input type="number" value={design.source_x} onChange={e=>patch('source_x',+e.target.value)}/></div><div className="field"><label>Source box</label><select value={design.source_bg} onChange={e=>patch('source_bg',e.target.value)}><option value="#24428e">Science Bee blue</option><option value="#000000">Black</option><option value="#ffffff">White</option><option value="transparent">None</option></select></div></div><div className="row3"><div className="field"><label>Source Y</label><input type="number" value={design.source_top} onChange={e=>patch('source_top',+e.target.value)}/></div><div className="field"><label>Logo</label><select value={design.logo} onChange={e=>patch('logo',e.target.value)}><option value="auto">Auto</option><option value="light">Light</option><option value="dark">Dark</option></select></div><div className="field"><label>Logo width</label><input type="number" value={design.logo_width} onChange={e=>patch('logo_width',+e.target.value)}/></div></div><div className="btnrow" style={{marginTop:8}}><button className="btn yellow" onClick={render} disabled={busy}>{busy?'Rendering…':'Render & save poster'}</button>{share&&<button className="btn" onClick={()=>navigator.clipboard.writeText(location.origin+share)}>Copy share link</button>}</div></div></div><div className="previewbox"><div style={{width:'100%'}}>{post.poster_url?<img className="posterimg" src={post.poster_url}/>:<div style={{color:'#fff',padding:40,textAlign:'center'}}>Render the poster to preview it here.</div>}</div></div></section>}</main></div>}
